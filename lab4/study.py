"JSON- текстовый формат для обмена данных"
"Данные здесь структурированы что даже похожи на словари в питоне"
# data = json.loads(xxx) для загрузки такого файла в тип питон 
# new_json = json.dumps(xxx)
# with open (# имя 'my.json', 'w' на записи) as  file:
            #json.dump(data,file,indent=3)
